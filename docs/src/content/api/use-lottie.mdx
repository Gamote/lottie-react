---
title: useLottie
description: Use Lottie React as a hook
---

import '../../styles.css';

```tsx:title=The&nbsp;file&nbsp;in&nbsp;which&nbsp;you're&nbsp;using&nbsp;the&nbsp;hook
const {
  View,
  play,
  stop,
  pause,
  setSpeed,
  goToAndStop,
  goToAndPlay,
  setDirection,
  playSegments,
  setSubframe,
  getDuration,
  destroy,
  animationLoaded,
  animationItem,
} = useLottie({
  data,
  renderer?,
  loop?,
  autoplay?,
  initialSegment?,
  // name?, // not tested
  // assetsPath?, // not tested
  // rendererSettings?, // not tested
  listeners?: {
    onComplete?,
    onLoopComplete?,
    onEnterFrame?,
    onSegmentStart?,
    onConfigReady?,
    onDataReady?,
    onDataFailed?,
    onLoadedImages?,
    onDOMLoaded?,
    onDestroy?,
  },
  containerProps?,
});

// or using the object syntax

const lottieObject = useLottie(config);
```

## Options

**`data: string | any`**
- **Required**
- The animation that will be used.
- If a string is passed, it will be treated as a path to the animation file.
- If anything other than string is passed, it will be treated as animation data.
- The animation will automatically reload when the path or data changes.

**`renderer?: 'svg' | 'canvas' | 'html'`**
- _Optional_
- **TODO**

**`loop?: boolean | number`**
- _Optional_
- **TODO**

**`autoplay?: boolean`**
- _Optional_
- **TODO**

**`initialSegment?: AnimationSegment`**
- _Optional_
- **TODO**

**`listeners?: LottieAnimationEvents`**
- _Optional_
- An object in which listeners, of type `AnimationEventHandler | null`, can be optionally defined for the following event keys:
  - `onComplete`
  - `onLoopComplete`
  - `onEnterFrame`
  - `onSegmentStart`
  - `onConfigReady`
  - `onDataReady`
  - `onDataFailed`
  - `onLoadedImages`
  - `onDOMLoaded`
  - `onDestroy`

**`containerProps?: React.HTMLProps<HTMLDivElement>`**
- _Optional_
- Object containing the properties that you want to pass to the `<div></div>` used as a container for the animation.
- **Attention:** The `ref` property will have no effect because it is used/overwritten internally by `lottie-react`.

## Returns

**`View:ReactElement`**
- **TODO**

**`play: () => void`**
- **TODO**

**`stop: () => void`**
- **TODO**

**`pause: () => void`**
- **TODO**

**`setSpeed: (speed: number) => void`**
- **TODO**

**`goToAndStop: (value: number, isFrame?: boolean) => void`**
- **TODO**

**`goToAndPlay: (value: number, isFrame?: boolean) => void`**
- **TODO**

**`setDirection: (direction: AnimationDirection) => void`**
- **TODO**

**`playSegments: (segments: AnimationSegment | AnimationSegment[], forceFlag?: boolean) => void`**
- **TODO**

**`setSubframe: (useSubFrames: boolean) => void`**
- **TODO**

**`getDuration: (inFrames?: boolean) => number | undefined`**
- **TODO**

**`destroy: () => void`**
- **TODO**

**`animationLoaded: boolean`**
- **TODO**

**`animationItem: AnimationItem | undefined`**
- **TODO**
